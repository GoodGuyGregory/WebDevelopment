const multiply = (x, y) => {
    return x * y;
}

const add = (a, b) => a + b;

const subtract = (a, b) =>  a - b;

// to practice more concise function syntax the return and curly braces can also be removed for these functions because they feature one line, the parentheses must remain because there are two arguments to these functions.